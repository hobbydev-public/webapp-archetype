package groupId.artifactId;

import groupId.artifactId.config.DataAccessConfig;
import groupId.artifactId.config.SwaggerConfig;

/**
 * Interface is primarily intended to designate the root package
 * for other application members.
 *
 * @see DataAccessConfig
 * @see SwaggerConfig
 */
public interface Application {}
